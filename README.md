# EmployeeManagement

## Adding Migrations

To add a migration, use the following command in the console:

```powershell
Add-Migration <MigrationName>
```

## Updating the Database

After adding a migration, use the following command to update the database:

```powershell
Update-Database
```